You are analysing competitor ads for the brand "$brand_name" in the "$category" category.

Below is a JSON array of competitor ads. For each ad, deconstruct the creative strategy and return a JSON array where each item has this exact structure:

{
  "ad_library_id": "<same id from input>",
  "psychological_trigger": "<one of: status, fear, social_proof, transformation, agitation_solution, curiosity, urgency, authority, belonging, aspiration>",
  "visual_layout": "<description of layout: e.g. 'before/after split', 'product hero', 'talking head', 'text overlay on lifestyle'>",
  "copy_tone": "<e.g. 'aspirational', 'clinical', 'conversational', 'urgent', 'empathetic'>",
  "reading_level": "<simple/intermediate/advanced>",
  "color_palette_json": {"primary": "<hex>", "secondary": "<hex>", "accent": "<hex>"},
  "is_profitable": <true if duration_days >= 21 else false>,
  "hook_formula": "<describe the hook pattern: e.g. 'problem statement', 'bold claim', 'question', 'social proof lead'>",
  "what_works": "<1-2 sentences on why this ad likely performs>",
  "weakness": "<1 sentence on the creative gap or fatigue risk>"
}

Input ads:
$ads_json

Return ONLY the JSON array. No markdown, no commentary.
